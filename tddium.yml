:tddium:
  :hooks:
    :pre: npm install && npm install grunt-cli karma bower && ./node_modules/bower/bin/bower install && npm install && bower install && grunt build
    :post: grunt ftp-deploy
  :nodejs:
    :version: '0.10.24'
  :tests:
    - grunt karma
