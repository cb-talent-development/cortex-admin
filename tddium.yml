:tddium:
  :hooks:
    :pre: npm install grunt-cli karma bower && npm install && ./node_modules/bower/bin/bower cache clean && ./node_modules/bower/bin/bower install && ./node_modules/grunt-cli/bin/grunt build
    :post: grunt ftp-deploy
  :nodejs:
    :version: '0.10.24'
  :tests:
    - ./node_modules/grunt-cli/bin/grunt karma
